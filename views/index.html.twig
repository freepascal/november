<!DOCTYPE html>
<html>
<title>
	{% if title %}
	{{ title }}
	{% endif %}
</title>
<body>

{# header #}
{% block header %}
	{% if logged_in %}
		<div id="logged_in">
			You are logged in as [{{ username }}]
		</div>
	{% else %}
		<div id="logged_in">
			You are not logged in. <a href="login">Login</a> now
		</div>
	{% endif %}
{% endblock %}
	
{# body #}
{# for post in posts #}	

{# footer #}
{% block footer %}
	Footer
{% endblock %}
</body>	
</html>
